// https://nuxt.com/docs/api/configuration/nuxt-config
export default defineNuxtConfig({
  devtools: { enabled: true }, // Enable devtools in dev mode
  pages: true, // Ensure that the pages directory is used for routing
})
